# Storage mode: local (browser localStorage, no login) | api (backend + JWT)
# Desktop (Tauri) builds always force local via npm run build:desktop / tauri:dev
VITE_STORAGE_MODE=local

# Required only when VITE_STORAGE_MODE=api
# VITE_API_URL=http://localhost:4000
